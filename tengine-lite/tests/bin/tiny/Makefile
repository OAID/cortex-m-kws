#only one generated object is permitted in one Makefile
gen-obj-y:=test_tiny_graph.o

#the sub objects to generate the object
sub-obj-y+=test_tiny.o
sub-obj-y+=tiny_graph_generated.o

COMMON_CFLAGS+=-I.

